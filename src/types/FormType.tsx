export type FormType = {
    inputs: Input[],
    textareas: TextArea[],
}

export type Input = {
    name: string,
    placeholder: string,
}

export type TextArea = {
    name: string,
    placeholder: string,
}